const API_URL = "https://ds-ai-wtqr.onrender.com";
const translations={en:{subtitle:"Ask questions, search for information, and chat with Nova AI.",placeholder:"Ask Nova anything..."},hi:{subtitle:"सवाल पूछें, जानकारी खोजें और Nova AI से चैट करें।",placeholder:"Nova से कुछ पूछें..."},pt:{subtitle:"Faça perguntas, pesquise informações e converse com a Nova AI.",placeholder:"Pergunte qualquer coisa à Nova..."},de:{subtitle:"Stelle Fragen, suche nach Informationen und chatte mit Nova AI.",placeholder:"Frag Nova etwas..."}};
const language=document.getElementById("language"),message=document.getElementById("message"),messages=document.getElementById("messages");
language.addEventListener("change",()=>{const x=translations[language.value];document.getElementById("subtitle").textContent=x.subtitle;message.placeholder=x.placeholder});
document.getElementById("chatForm").addEventListener("submit",async e=>{e.preventDefault();const text=message.value.trim();if(!text)return;addMessage(text,"user");message.value="";try{const r=await fetch(`${API_URL}/chat`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:text,language:language.value})});const d=await r.json();if(!r.ok)throw Error(d.detail);addMessage(d.reply,"bot")}catch{addMessage("Nova could not connect to the server.","bot")}});
function addMessage(text,type){const el=document.createElement("div");el.className=`message ${type}`;el.textContent=text;messages.appendChild(el);messages.scrollTop=messages.scrollHeight}
document.getElementById("reportForm").addEventListener("submit",async e=>{e.preventDefault();const report=document.getElementById("reportMessage").value.trim(),page=document.getElementById("reportPage").value.trim(),status=document.getElementById("reportStatus");if(!report)return;try{const r=await fetch(`${API_URL}/report`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:report,page:page||"website"})});const d=await r.json();if(!r.ok)throw Error(d.detail);status.textContent="Thanks! Your report was sent.";document.getElementById("reportForm").reset()}catch{status.textContent="Could not send the report."}});
